## 1. Skill 包基础结构

- [ ] 1.1 在 `greeting-generator/` 初始化 Skill 包目录与入口说明文件（含执行目标、输入输出契约）。
- [ ] 1.2 建立可复用的字段定义与默认值常量（节日、用户画像、用途、候选数、发送对象结构）。
- [ ] 1.3 增加示例输入文件（JSON/YAML/Markdown 至少各 1 个）用于手工验证和回归。

## 2. 交互式采集流程实现

- [ ] 2.1 实现节日信息采集步骤，强制要求 `festival_name`，缺失时阻塞进入下一步。
- [ ] 2.2 实现用户画像采集步骤，明确“可选输入 + 质量影响提示”。
- [ ] 2.3 实现用途与规格采集步骤，支持场景、长度、候选数量，候选数量默认 `3`。
- [ ] 2.4 增加流程状态对象，统一保存采集结果并供后续生成阶段消费。

## 3. 发送对象解析与规范化

- [ ] 3.1 实现对话输入的发送对象抽取与标准化映射（name/title/relation/age/gender/notes）。
- [ ] 3.2 实现文件路径读取与多格式解析（Markdown/JSON/YAML/txt）。
- [ ] 3.3 实现缺失字段回退策略（含“佚名”占位与低个性化提示）。
- [ ] 3.4 对批量对象输入增加解析结果预览，允许用户确认后继续生成。

## 4. 祝福语生成与渲染

- [ ] 4.1 实现按对象逐个生成候选文案的流程，确保总量满足 `对象数 x 候选数`。
- [ ] 4.2 实现同对象候选差异化策略（语气/重点/句式）并增加近似重复检查。
- [ ] 4.3 实现固定 Markdown 输出模板（上下文块 + 按对象分组候选列表）。
- [ ] 4.4 实现多对象分组渲染，保证每个对象的候选数量与顺序稳定。

## 5. 导出与验证

- [ ] 5.1 实现“是否写入文件”交互，支持用户自定义文件名。
- [ ] 5.2 实现默认导出文件名规则：`greeting-generator-<YYYYMMDD-HHMMSS>.md`。
- [ ] 5.3 增加端到端验收用例：单对象默认候选、多对象批量导入、拒绝画像、拒绝写文件。
- [ ] 5.4 运行类型检查与规范检查，修复发现的问题并记录验收结论。
